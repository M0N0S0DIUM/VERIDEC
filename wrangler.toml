# Cloudflare Workers Configuration

name = "veridec"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# Environment variables (set these in Cloudflare dashboard)
[vars]
MCP_SERVER_URL = "http://localhost:3000"  # Change to your MCP server URL